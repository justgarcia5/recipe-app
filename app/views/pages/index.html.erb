<% if user_signed_in? %>
  <%= react_component "Authenticated", current_user: {
    id: current_user.id,
    email: current_user.email,
  } %>
<% else %>
  <%= react_component 'Unauthenticated', props: {'logged_in': false} %>
<% end %>
